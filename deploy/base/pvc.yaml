apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nebulaos-data
  labels:
    app.kubernetes.io/name: nebulaos
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

